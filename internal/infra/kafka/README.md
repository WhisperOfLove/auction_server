# Kafka integration (next step)

This folder will contain:
- event producer for bid_accepted, auction_closed, message_sent
- consumer workers for notifications/analytics
- dead-letter handling and retry policy

For now, server uses direct in-process flow for local development.
